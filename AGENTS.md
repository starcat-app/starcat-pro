# AGENTS.md

本文档是 `starcat-pro` 的 AI 协作规则唯一维护源。

## 仓库边界

- 本目录是 Starcat 的公开支持、产品说明与发布日志仓库，不包含应用源码、后端源码、
  构建脚本、签名材料或私有配置。
- 本仓库拥有独立分支、remote、提交和发布边界；不得把改动并入外层 Starcat 主仓库
  或顺带修改相邻 `supports/*` 项目。
- 开工前核对 `git status --short` 与当前分支，保留用户已有改动；未经明确要求不
  commit、push、切分支、创建 Release 或修改 remote。

## 用途、内容与关键文件

- `README.md` 与 `README-ZH.md` 分别是英文、简体中文产品与支持入口。
- `CHANGELOG.md` 与 `CHANGELOG-ZH.md` 分别是英文、简体中文公开发布日志。
- 根目录 `banner.webp` 与 `main.webp` 是 Starcat 生态 README 共同引用的固定公开图片。
- 仓库技术形态是 Markdown 与静态图片，不在此实现产品功能，也不把内部设计、未公开
  路线图或私有仓库路径写入公开文档。

## 双语与图片规则

- 修改功能事实、版本号、下载链接、兼容性、限制或发布条目时，必须同步检查对应的
  中英文文件；两种语言保持版本、条目范围和图片位置一致，文字使用自然表达。
- Changelog 按版本倒序维护，并保持“新增 / 优化 / 修复”等分类与英文对应分类语义一致；
  未发布内容必须明确标记，不能写成已交付事实。
- `banner.webp` 与 `main.webp` 的文件名和根目录路径是跨仓库公共契约。未经 dong4j
  明确批准，不得移动、重命名、删除、替换或重新编码，也不得把 README 引用改成临时路径。
- 新增截图应使用已确认的长期公开路径，去除账户、Token、私有仓库和其他用户数据。

## 验证命令

```bash
git diff --check
git status --short
```

纯文档仓库没有构建门禁；交付前还必须人工对照中英文版本号、标题层级、链接、图片 URL
与 `banner.webp` / `main.webp` 固定引用。

## 外部副作用禁令

- 未经明确授权，不得 push、发布 GitHub Release、上传图片、改 Issue 设置或公开尚未
  发布的功能与日期。
- 不得从私有 Starcat 仓库复制源码、凭据、内部日志或未脱敏截图到本仓库。
